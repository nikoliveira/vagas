// Define user permissions
const permissions = {
    canDeleteUsers: false, // Determines if the user has permission to delete users
    canUpdateUsers: false, // Determines if the user has permission to update users
};

module.exports = permissions;
